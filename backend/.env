PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:3000
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=""
DB_NAME= inventory

# ── Password Reset / Email Settings ─────────────────────────────────────────
# App URL (where the Nuxt frontend runs — used in reset email link)
APP_URL=http://localhost:3000

# SMTP Email (Mailtrap)
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_SECURE=false
SMTP_USER=b210dd98db53ad
SMTP_PASS=874b19f03321bf
EMAIL_FROM="PharmaCare SaaS <no-reply@pharmacare.com>"

# Reset token expiry (in minutes)
RESET_TOKEN_EXPIRY_MINUTES=30